import React from 'react';
import PlaceholderScreen from '../../components/PlaceholderScreen';

const CreateTradeOfferScreen: React.FC = () => {
  return (
    <PlaceholderScreen
      title="Create Trade Offer"
      subtitle="Make an offer to another collector"
      iconName="add-business"
    />
  );
};

export default CreateTradeOfferScreen;